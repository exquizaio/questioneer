require 'spec_helper'

describe QuestionMapper do
  it 'has a version number' do
    expect(QuestionMapper::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
